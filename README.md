A tool for selecting different Android tool sets.

    Usage:  android-select -s
            android-select -v
    Arguments:

            -s <android_folder_path>   the path for the current Xcode folder
            -v android-select version information
